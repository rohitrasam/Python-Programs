t = int(input())
for i in range(t):
    try:
        ab = input().split()
        a = int(ab[0])
        b = int(ab[1])
        print(a // b)
    except ZeroDivisionError as e:
        print("Error Code:", e)
    except ValueError as e:
        print("Error Code:", e)
